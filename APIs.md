# Frontend API calls

This file used to list the endpoints the workflow editor needed. Most of them
no longer exist.

- **Every backend route** is documented in
  [`../Backend/docs/API.md`](../Backend/docs/API.md), which is kept up to date.
- **The frontend's calls** live in `src/api/`, one file per backend area. That
  folder is the only place the app talks to the backend.

The old list is in git history.
